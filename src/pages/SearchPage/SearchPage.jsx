// Dependecies
import styled from "styled-components";

function SearchPage() {
    return <Wrapper>SearchPage</Wrapper>;
}

// Styled Components

const Wrapper = styled.div``;

export default SearchPage;
